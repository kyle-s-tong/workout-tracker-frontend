{{!-- TODO: Decide if we want to add in exercise summaries here or somewhere else  --}}
{{!-- {{#swiper-container
  class="flex flex-auto w-full h-full"
}}
    {{#swiper-slide}}
      <div class="w-full h-full">
        hi
      </div>
    {{/swiper-slide}}
    {{#swiper-slide}}
      <div class="w-full h-full">
        hi 2
      </div>
    {{/swiper-slide}}
{{/swiper-container}} --}}

<div class="h-screen flex flex-col items-center">
  {{title-section title="Add an exercise"}}
  <div class="w-1/2">
    {{#form-for model classNames="form" as |exercise|}}
      {{exercise.text-field "title" fieldClasses="form-field" inputClasses="form-input" labelClasses="form-label"}}
      {{exercise.number-field "numberOfSets" fieldClasses="form-field" inputClasses="form-input" labelClasses="form-label"}}
      {{exercise.number-field "reps" fieldClasses="form-field" inputClasses="form-input" labelClasses="form-label"}}
      {{exercise.number-field "rest" fieldClasses="form-field" inputClasses="form-input" labelClasses="form-label"}}

      {{!-- Add a custom label because this isn't part of form for --}}
      <div class="form-label self-start pt-2">
        Superset
      </div>
      {{#power-select-multiple
        options=model.workout.exercises
        selected=model.superset
        placeholder="Select supersets"
        onchange=(action (mut model.superset))
        as |supersetExercise|
      }}
        {{supersetExercise.title}}
      {{/power-select-multiple}}

      {{exercise.submit "Save exercise" class="form-button" action=(action "save" model)}}
    {{/form-for}}
  </div>
</div>